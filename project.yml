name: CraftLink
options:
  bundleIdPrefix: com.craftlink
  deploymentTarget:
    iOS: 15.0
  xcodeVersion: "15.0"
settings:
  base:
    SWIFT_VERSION: 5.0
    ENABLE_BITCODE: NO
targets:
  CraftLink:
    type: application
    platform: iOS
    sources:
      - path: .
        excludes:
          - "PacketTunnel"          # 排除扩展目录
          - "Core"
          - "RustLib"
          - ".github"
          - "CraftLink.xcodeproj"
          - "*.md"
          - "*.yml"
          - ".*"
          - "project.yml"
          - "LICENSE"
          - "README.md"
        includes:
          - "App/**/*.swift"
          - "Views/**/*.swift"
          - "Services/**/*.swift"
          - "Models/**/*.swift"
          - "Utils/**/*.swift"
          - "ViewModels/**/*.swift"
          - "Info.plist"
          - "CraftLink.entitlements"
    entitlements:
      path: CraftLink.entitlements
    info:
      path: Info.plist
      properties:
        CFBundleIdentifier: $(PRODUCT_BUNDLE_IDENTIFIER)
        CFBundleName: CraftLink
        CFBundleDisplayName: 陶瓦联机
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.craftlink.app
        LIBRARY_SEARCH_PATHS: $(PROJECT_DIR)/RustLib
        OTHER_LDFLAGS: -all_load -leasytier_ios
    dependencies:
      - target: PacketTunnel        # 改为新名称
  PacketTunnel:                     # 新 target 名称
    type: app-extension
    platform: iOS
    sources:
      - path: PacketTunnel          # 注意：原 NetworkExtension 目录需要重命名为 PacketTunnel
      - path: Services/InviteCodeService.swift
      - path: Services/RustBridge.swift
    entitlements:
      path: PacketTunnel/PacketTunnel.entitlements   # 对应重命名
    info:
      path: PacketTunnel/Info.plist                  # 对应重命名
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.craftlink.packet-tunnel
        SKIP_INSTALL: YES
        LIBRARY_SEARCH_PATHS: $(PROJECT_DIR)/RustLib
        OTHER_LDFLAGS: -all_load -leasytier_ios -framework NetworkExtension
    dependencies:
      - sdk: NetworkExtension.framework
      - sdk: Network.framework
      - sdk: Security.framework