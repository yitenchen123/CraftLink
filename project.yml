name: CraftLink
options:
  bundleIdPrefix: com.craftlink
  deploymentTarget:
    iOS: 15.0
  xcodeVersion: "15.0"
settings:
  base:
    SWIFT_VERSION: 5.0
    ENABLE_BITCODE: NO
targets:
  CraftLink: # 主App Target
    type: application
    platform: iOS
    sources:
      - path: .
        excludes:
          - "NetworkExtension"
          - "Core"
          - "RustLib"
          - ".github"
          - "CraftLink.xcodeproj"
          - "*.md"
          - "*.yml"
          - ".*"
          - "project.yml"
          - "LICENSE"
          - "README.md"
        includes:
          - "App/**/*.swift"
          - "Views/**/*.swift"
          - "Services/**/*.swift"
          - "Models/**/*.swift"
          - "Utils/**/*.swift"
          - "ViewModels/**/*.swift"
          - "Info.plist"
          - "CraftLink.entitlements"
    entitlements:
      path: CraftLink.entitlements
    info:
      path: Info.plist
      properties:
        CFBundleIdentifier: $(PRODUCT_BUNDLE_IDENTIFIER)
        CFBundleName: CraftLink
        CFBundleDisplayName: 陶瓦联机
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.craftlink.app
        LIBRARY_SEARCH_PATHS: $(PROJECT_DIR)/RustLib
        # 正确链接静态库，-leasytier_ios 对应 libeasytier_ios.a
        OTHER_LDFLAGS: -all_load -leasytier_ios
    dependencies:
      - target: NetworkExtension
  NetworkExtension: # VPN扩展 Target
    type: app-extension
    platform: iOS
    sources:
      - path: NetworkExtension
      # ★★★ 关键修复：显式添加缺失的源文件 ★★★
      - path: Services/InviteCodeService.swift
      - path: Services/RustBridge.swift
    entitlements:
      path: NetworkExtension/NetworkExtension.entitlements
    info:
      path: NetworkExtension/Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.craftlink.network-extension
        SKIP_INSTALL: YES
    dependencies:
      - sdk: Network.framework
      - sdk: Security.framework